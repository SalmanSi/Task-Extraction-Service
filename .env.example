# Copy this file to .env and fill in the keys you intend to use

# OpenAI (set if using provider="openai")
OPENAI_API_KEY=sk-replace-me

# Gemini (set if using provider="gemini")
GEMINI_API_KEY=replace-me

# Optional future variable to control provider selection dynamically
# PROVIDER=openai
